const ApplicationPolicy = require("./application");

module.exports = class CommentPolicy extends ApplicationPolicy {
  //the create and destroy methods of the parent class meet our needs, so we don't add code here
};
